#include <stdio.h>

/**
 * main - prints the lowercase alphabet in reverse
 *
 * Return: 0 (sucess)
 */
int main(void)
{
	char kc;

	for(kc = 'z'; && kc <= 'a'; kc--)
	{
		putchar(kc);
	{
	putchar('\n');
	return (0);
}
